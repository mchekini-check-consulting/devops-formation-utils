apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: stress-cpu-front
  namespace: dev
spec:
  mode: one
  selector:
    namespaces:
      - dev
    labelSelectors:
      app: front
  stressors:
    cpu:
      workers: 2
      load: 100
  duration: "5m"
